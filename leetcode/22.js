// todo

/**
 * @param {number} n
 * @return {string[]}
 */
var generateParenthesis = function(n) {
  const point = (n-1)*2;
  const index = n
  if(point===0) return 1;
  
};

var test = function(n,m) {
  if(n<m) return -1;
  let arr = [...Array(m).keys()]
  
}